name: Build and Deploy code
on: [push, pull_request]
jobs:
  job1:
    runs_on: windows-latest
    steps:
      - name: pulling git repo
      - uses: actions/checkout@v2
      - name: hello snajeev
        run: echo'heello sanjeev'
